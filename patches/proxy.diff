diff --git a/linux/net/core/neighbour.c b/linux/net/core/neighbour.c
index c56a4e7b..e7ba8fa6 100644
--- a/linux/net/core/neighbour.c
+++ b/linux/net/core/neighbour.c
@@ -471,7 +471,8 @@ static int __neigh_ifdown(struct neigh_table *tbl, struct net_device *dev,
 	}
 	spin_unlock_bh(&tbl->lock);
 
-	pneigh_ifdown(tbl, dev, skip_perm);
+	if (!skip_perm)
+		pneigh_ifdown(tbl, dev, skip_perm);
 	pneigh_queue_purge(&tbl->proxy_queue, dev ? dev_net(dev) : NULL,
 			   tbl->family);
 	if (skb_queue_empty_lockless(&tbl->proxy_queue))
